apiVersion: v1
kind: Service
metadata:
  name: {{ include "ai-chat.fullname" . }}
  labels:
    {{- include "ai-chat.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    app.kubernetes.io/component: app
    {{- include "ai-chat.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
